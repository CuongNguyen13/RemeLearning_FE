# bff-service base path. In dev this is proxied by vite.config.ts to avoid bff-service's missing CORS config.
VITE_API_BASE_URL=/api/v1

# user-service is called directly only for the avatar upload endpoint, which bff-service doesn't proxy.
VITE_USER_SERVICE_URL=http://localhost:8081
